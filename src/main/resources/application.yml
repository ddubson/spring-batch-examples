spring:
  main:
    banner-mode: "off"
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/batch_job_repository
    username: root
    schema: /schema-mysql.sql
logging:
  level:
    root: WARN
    org.springframework.batch: WARN
